# dataverse-swa-dashboard

Azure Static Web App dashboard for Microsoft Dataverse (SWA + Entra ID).

> Greenfield scaffold — see `docs/` and `brief.md` for architecture.

## Quick start

See `api/README.md` for API layer, and root docs after frontend/infra land.

